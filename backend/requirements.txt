# 01 Capital Backend — Python Dependencies

# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic[email]==2.11.2
pydantic-settings==2.9.1

# Database
sqlalchemy[asyncio]==2.0.40
asyncpg==0.30.0
alembic==1.15.2

# Auth
bcrypt==4.3.0
PyJWT==2.10.1
pyotp==2.9.0
qrcode[pil]==8.1.0
cryptography==44.0.3

# Rate limiting
slowapi==0.1.9

# Date utilities
python-dateutil==2.9.0.post0

# Document generation (bilingual PDF). WeasyPrint needs native libs at runtime
# (libpango, libcairo, libgdk-pixbuf) — installed in the Dockerfile. It is
# imported lazily so the app and test suite run even where those libs are absent.
# pydyf is pinned: WeasyPrint 62.3 calls pydyf APIs removed in pydyf 0.11+
# (AttributeError: 'super' object has no attribute 'transform' at render time).
weasyprint==62.3
pydyf==0.10.0
Jinja2==3.1.4

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.1.0
httpx==0.28.1
